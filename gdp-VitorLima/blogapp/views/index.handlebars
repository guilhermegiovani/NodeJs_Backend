<div class="p-5 mb-4 bg-light rounded-3">
    <div class="container-fluid py-2">
        <h1 class="display-5 fw-bold">Bem-vindo ao Blog do Node!</h1>
        <p class="col-md-8 fs-4">Texto introdutório com mais espaçamento e destaque visual.</p>
        <button class="btn btn-primary btn-lg" type="button">Crie uma conta</button>
    </div>
</div>

<h2>Postagens recentes: </h2>

{{#each postagens}}
    <div class="card my-4">
        <div class="card-body">
            <h3>{{titulo}}</h3>
            <p>{{descricao}}</p>
        </div>
    </div>
{{else}}
    <h5>Nenhuma postagem</h5>
{{/each}}